# setup.py in sheet_metal_unfolding_project directory
from setuptools import setup, find_packages

setup(
    name='sheet_metal_unfolding_project',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # Add your dependencies here
    ],
)
